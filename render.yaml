# One-click deploy on Render: https://render.com/docs/infrastructure-as-code
services:
  - type: web
    name: gamecam
    runtime: node
    plan: free
    buildCommand: npm ci
    startCommand: node server.js
    healthCheckPath: /
